// TODO: Add your plugin's utility functions here.
// Keep this file for reusable helpers that support your plugin logic.
//
// Example:
//   export function formatServiceName(name: string): string {
//     return name.replace(/[^a-zA-Z0-9]/g, '-');
//   }
